require 'rubygems'
require 'rake'

begin
  require 'jeweler'

  Jeweler::Tasks.new do |spec|
    spec.name                   = "ajaxful_rating"
    spec.version                = "3.1.0.beta1"
    spec.summary                = "Provides a simple way to add rating functionality to your application."
    spec.description            = "Provides a simple way to add rating functionality to your application."
    spec.homepage               = "http://github.com/tecnh/ajaxful-rating"
    spec.authors                = ["Mat Ellis", "Jack Chu", "Edgar J. Suarez", "Denis Odorcic"]
    spec.email                  = ["mat@tecnh.com", "jack@jackchu.com", "edgar.js@gmail.com", "denis.odorcic@gmail.com"]
    spec.has_rdoc               = true
    spec.files.exclude          'init.rb'
  end

rescue LoadError
  puts "Jeweler not available. Install it with: gem install jeweler"
end
  s.email =
